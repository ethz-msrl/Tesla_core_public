name: CardioMag
type: backward_model_nls
forward_model:
  type: mpem
  filename: CardioMag_CalibrationCube_03-04-19.yaml 
nls:
  # optional params to define parameters of the nonlinear least squares
  do_print_summary: false
  do_minimize_currents: true
  do_cache_position: true
  weight_currents: 1e-8
  max_field_dist: 100e-6

# this should identify the config file version
# for future changes
version: 1.0
